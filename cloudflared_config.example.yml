tunnel: YOUR_TUNNEL_ID
credentials-file: <BASE_DIR>/cloudflared/YOUR_TUNNEL_ID.json

ingress:
  - hostname: MACHINE_NAME.guard.example.com
    service: http://127.0.0.1:5555
  - service: http_status:404
